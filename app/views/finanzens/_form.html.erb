<%= form_for(@finanzen) do |f| %>
  <% if @finanzen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@finanzen.errors.count, "error") %> prohibited this finanzen from being saved:</h2>

      <ul>
      <% @finanzen.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :datum %><br />
    <%= f.date_select :datum %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
